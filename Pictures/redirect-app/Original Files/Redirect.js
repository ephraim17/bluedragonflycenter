function setCookie(cname, cvalue, exdays) {
    var d = new document.referrer.();
    if d = account/login {

    }
    d.setTime(d.getTime() + (exdays*24*60*60*1000));
    var expires = "expires="+ d.toUTCString();
    document.cookie = cname + "=" + cvalue + ";" + expires + ";path=/";
  }


//  <script>
  (function() {
    var REDIRECT_PATH = '/404';

    var selector = '#create_customer, form[action$="/account"][method="post"]',
        $form = document.querySelectorAll(selector)[0];

    if ($form) {
      $redirect = document.createElement('input');
      $redirect.setAttribute('name', 'return_to');
      $redirect.setAttribute('type', 'hidden');
      $redirect.value = REDIRECT_PATH;
      $form.appendChild($redirect);
    }
  })();
//</script>

//<script>

var retrievedObject = localStorage.getItem('Test');

console.log('retrievedObject: ', JSON.parse(retrievedObject));
  
var theUrl = JSON.parse(retrievedObject)["pageurl"];

  (function() {
    var REDIRECT_PATH = '$document.referer';

    var selector = '#create_customer, form[action$="/account"][method="post"]',
        $form = document.querySelectorAll(selector)[0];

    if ($form) {
      $redirect = document.createElement('input');
      $redirect.setAttribute('name', 'return_to');
      $redirect.setAttribute('type', 'hidden');
      $redirect.value = REDIRECT_PATH;
      $form.appendChild($redirect);
    }
  })();
//</script>

document.cookie 

document.cookie = var referrer = document.referrer;

function setCookie() {
    document.referrer
  }
var referrer = document.referrer;


function passValues()
{ __st;
    localStorage.setItem("textvalue",__st);
    return false; 
}

//Code to store URL data in local storage

//<script>

var Test = __st;
localStorage.setItem('Test', JSON.stringify(Test));

//</script>

//code to retrieve data

//<script>
var retrievedObject = localStorage.getItem('Test');

			console.log('retrievedObject: ', JSON.parse(retrievedObject));
  
			var theUrl = JSON.parse(retrievedObject)["pageurl"];
              var url_dec = decodeURIComponent(theUrl);
              
              var checkout = url_dec.replace('qu-ds.com/account/login?checkout_url=', '');
              var newCheckout = url_dec.replace('browser-security.myshopify.com', 'qu-ds.com');


window.location.href =  '"' + checkout + '"';

function redirect(checkout) {
  window.location.href(checkout);
}

redirect();


https://browser-security.myshopify.com/44514017443/checkouts/6816a7755408f96d90a8578350a30ddf?key=e9d7ca47e4541f683f821016fef0cf64
https://qu-ds.com/44514017443/checkouts/6816a7755408f96d90a8578350a30ddf